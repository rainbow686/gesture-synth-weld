// Environment configuration
// Set VITE_ENABLE_ADS=true in .env to enable AdSense

export const ENABLE_ADS = import.meta.env.VITE_ENABLE_ADS === 'true';

// AdSense publisher ID (replace with your real ID after approval)
export const ADSENSE_CLIENT_ID = 'ca-pub-XXXXXXXXXXXXXXXX';
